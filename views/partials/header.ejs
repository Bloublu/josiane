<header>
    <div class="header_container">
        <div class="header_img"><img src="./images/Josiane.png" alt="jojo"></div>
        <div class="header_title"><h1>Chez Josiane</h1></div>
        <div class="header_ahref">
            <!-- faire condition pour afficher les bons liens en fonction si l'utilisateur est connecté ou pas (ex deconnexion)-->
            <% if (session.User) {%>
                <a href="/disconnect">Se Deconnecter</a>
            <% } %>
            <% if (!session.User) {%>
                <a href="/login">connexion</a>
            <div class="separateur"> / </div>
            <a href="/signup">s'inscrire</a>
            <% } %>
        </div>
        <div class="header_user">
            <% if (session.User) {%>
                <p>Bienvenue <strong><%= session.User.pseudo %> </strong> 
                    <a href="/params"><img style="height: 25px; width: auto; padding-left: 10px;"src="./images/moncompte.png" alt="moncompte"></a>
                
                <% if (session.User.adm == true) { %>
                    <a href="/usersAdm"><img style="height: 25px; width: auto; padding-left: 10px;"src="./images/users.png" alt="users"></a>
                <% } %> 
                </p>
            <% } %> 
        </div>
    </div>
</header>